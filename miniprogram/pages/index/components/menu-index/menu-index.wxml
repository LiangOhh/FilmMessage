<view class="container">
  <view class="list">
    <view class="item" wx:for="{{list}}" wx:key="id" bind:tap="test" data-data="{{item}}">
      <i-icon class="icon" name="{{item.icon}}"></i-icon>
      <text class="name">{{item.name}}</text>
    </view>
  </view>
</view>